---
- name: Install useful snap packages
  community.general.snap:
    name: "{{ item.name }}"
    channel: "{{ item.channel | default('stable') }}"
    classic: "{{ item.classic | default('no') }}"
    state: present
  loop: "{{ useful_snap_packages }}"
  loop_control:
    label: "{{ item.name }}"
