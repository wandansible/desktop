---
- name: systemd daemon-reload
  systemd:
    daemon_reload: yes

- name: restart docker
  service:
    name: docker
    state: restarted

- name: apply display manager
  command: dpkg-reconfigure -f noninteractive {{ desktop_dm }}
